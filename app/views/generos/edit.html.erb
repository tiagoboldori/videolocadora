<h1>Editar Gênero</h1>

<%= form_with(model: @genero) do |form| %>
  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, required: true %>
  </div>

  <div class="actions">
    <%= form.submit "Atualizar", class: "button is-primary" %>
  </div>
<% end %>
